import React from 'react';
import { CharactersList } from '@/entities/Character';

const CharacterPage = () => {
	return (
		<div>
			<CharactersList />
		</div>
	);
};

export default CharacterPage;
